// ler os elementos de uma matriz
for(i) // linhas
	for(j) // colunas
		scanf();
		
for(i) // linhas
	for(j) // colunas
		printf();		
		
for(i) // linhas
	for(j) // colunas
		b[ij] += a[ij];		
		
for(i) // linhas
	for(j) // colunas
		b[ij] -= a[ij];		
		
		
		
		
		
		
