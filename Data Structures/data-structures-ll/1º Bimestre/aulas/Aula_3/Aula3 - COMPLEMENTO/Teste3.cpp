#include <stdio.h>

int a=7, b=4, c=9;
int v[3];

main() {
	// bubble sort - método bolha
	for() // 1 dim
	   for() // 2 dim
	      if(v[i] > v[i+1]) {
	      	troca = v[i];
	      	v[i] = v[i+1];
	      	v[i+1] = troca;
		  }
	      	
}
