#include <stdio.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{
    
    return 0;
}

/*
marque(s);
enfileire(Q, s);
enquanto Q for nulo faça
    u <- desenfileire(Q)
    processe(u)
    enquanto houver v não marcado adjacente de u faça
        marque(v)
        enfileire(Q, v)
*/
