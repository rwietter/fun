fn operators() {
    let x = 5 + 5; // addition
    let y = 5 - 5; // subtraction
    let z = 5 * 5; // multiplication
    let w = 5 / 5; // division
    let r = 5 % 5; // remainder
    let b = 5 == 5; // equality
    let b = 5 != 5; // inequality
    let b = 5 > 5; // greater than
    let b = 5 < 5; // less than
    let b = 5 >= 5; // greater than or equal to
    let b = 5 <= 5; // less than or equal to
    let b = true && false; // logical and
    let b = true || false; // logical or
    let b = !true; // logical not
    let b = 5 & 5; // bitwise and
    let b = 5 | 5; // bitwise or
    let b = 5 ^ 5; // bitwise xor
    let b = 5 << 5; // bitwise shift left
    let b = 5 >> 5; // bitwise shift right
    let b = 5 as i64; // type cast
    let b = 5.0 as i64; // type cast
    let b = 5 as f64; // type cast
    let b = 5.0 as f64; // type cast
    let b = 5 as f64 + 5.0; // type cast
    let b = 5.0 as i64 + 5; // type cast
    let b = 5.0 as i64 + 5.0 as i64; // type cast
}
